version: '3.8'

services:
  backend:
    build:
      context: ./
      dockerfile: ./Dockerfile
    ports:
      - "8001:8001"
    depends_on:
      - db
    env_file:
      - .env
    restart: always

  db:
    image: postgres
    ports:
      - "5432:5432"
    env_file:
      - .env


