version: '3.8'

services:
  backend:
    build:
      context: ./
      dockerfile: ./Dockerfile
    ports:
      - "8001:8001"
    depends_on:
      - db
    env_file:
      - .env
    restart: always
    networks:
      - my_network

  db:
    image: postgres
    ports:
      - "5432:5432"
    env_file:
      - .env
    networks:
      - my_network

  adminer:
    image: adminer
    restart: always
    ports:
      - "8002:8080"
    depends_on:
      - db
    networks:
      - my_network


networks:
  my_network:


